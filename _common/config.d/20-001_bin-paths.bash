#!/usr/bin/env bash

##
# This file is part of `src-run/usr-src-builder`
#
# (c) Rob Frawley 2nd <rmf@scr.be>
#
# For the full copyright and license information, view the LICENSE.md
# file distributed with this source code.
##

export _BIN_PECL="$(which pecl)"
export _BIN_CURL="$(which curl)"
export _BIN_TAR="$(which tar)"
export _BIN_MAKE="$(which make)"
export _BIN_GIT="$(which git)"
export _BIN_PHP="$(which php)"
export _BIN_PHPENV="$(which phpenv)"
export _BIN_PHPIZE="$(which phpize)"
export _BIN_HHVM="$(which hhvm)"
